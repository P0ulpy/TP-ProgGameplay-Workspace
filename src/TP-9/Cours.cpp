//
// Created by Flo on 29/04/2022.
//

#include <iostream>
/*
template<typename Type1, typename Type2>
struct Pair
{
    Pair(const Type1& f, const Type2& s)
            : first(first)
            , second(second)
    {
        std::cout << first << "Generic constrcut" << second << std::endl;
    }
    Type1 first;
    Type2 second;
};

template<>
struct Pair<int, Type2>

template<typename Type>
struct Pair<Type, Type>
{
    Pair(const Type& f, const Type& s)
        : first(first)
        , second(second)
    {
        std::cout << "same types used" << std::endl;
    }
    T1 first;
    T2 second;
};

template<typename Type>
struct Pair<Type, Type>
{
    Pair(const Type& f, const Type& s)
            : first(first)
            , second(second)
    {
        std::cout << "same types used" << std::endl;
    }
    T1 first;
    T2 second;
};
 */